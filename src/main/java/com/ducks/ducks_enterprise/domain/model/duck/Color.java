package com.ducks.ducks_enterprise.domain.model.duck;

import com.ducks.ducks_enterprise.domain.shared.ValueObject;

public enum Color implements ValueObject<Color> {
    WHITE, BROWN, DARK, GREEN, PINK;
}
